apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kube-system

helmCharts:
- name: cilium
  namespace: kube-system
  includeCRDs: true
  releaseName: rke2-cilium
  # version: latest
  repo: https://rke2-charts.rancher.io/
  # valuesFile: values.yaml

